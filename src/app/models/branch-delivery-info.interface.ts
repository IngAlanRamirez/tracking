/**
 * Información de la sucursal o dirección de entrega
 */
export interface BranchDeliveryInfo {
  branchIdent?: string; // Identificador de sucursal (opcional)
  branchName: string; // Nombre de sucursal o dirección completa
}
